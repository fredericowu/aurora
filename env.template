# =============================================================================
# Aurora Search Engine - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in your actual values:
#   cp env.template .env
# Then edit .env with your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (for ingestion script and Lambda)
# -----------------------------------------------------------------------------
# RDS PostgreSQL connection details
# Get these from Terraform outputs after deployment:
#   terraform -chdir=terraform output rds_endpoint
#   terraform -chdir=terraform output rds_port
#   terraform -chdir=terraform output rds_database_name
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432
DB_NAME=messages
DB_USER=postgres
DB_PASSWORD=your-secure-password-here

# -----------------------------------------------------------------------------
# API Configuration (for performance testing script)
# -----------------------------------------------------------------------------
# API Gateway endpoint URL
# Get this from Terraform outputs after deployment:
#   terraform -chdir=terraform output api_gateway_url
API_BASE_URL=https://your-api.execute-api.region.amazonaws.com

# -----------------------------------------------------------------------------
# AWS Configuration (for local Terraform/script execution)
# -----------------------------------------------------------------------------
# AWS credentials (if running scripts locally)
# Note: GitHub Actions uses GitHub Secrets instead
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_REGION=us-east-1

# -----------------------------------------------------------------------------
# Terraform Variables (if running Terraform locally)
# -----------------------------------------------------------------------------
# Terraform will read these as TF_VAR_* environment variables
# Or pass via -var flags: terraform apply -var="db_password=your-password"
# TF_VAR_db_password=your-secure-password-here
# TF_VAR_aws_region=us-east-1
# TF_VAR_db_instance_class=db.t3.micro

# -----------------------------------------------------------------------------
# Optional: External API Configuration
# -----------------------------------------------------------------------------
# The ingestion script uses this API endpoint
# EXTERNAL_API_URL=https://november7-730026606190.europe-west1.run.app

